[project]
name = "post-trade"
version = "0.1.0"
description = "post-trade notebooks over the parquet a strategy run records"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "polars>=1.42.1",
]
